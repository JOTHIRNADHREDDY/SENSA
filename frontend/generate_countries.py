import json
import urllib.request

# We will use restcountries.com or similar, but since we are in a sandbox it's safer to just generate it from a known list.
# Actually I'll use python's pycountry if available, but it's not. 
# I will use a python script that fetches the countries from a reliable open API.
